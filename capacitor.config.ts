import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.tamano_torcho_app',
  appName: 'tamaño-torcho-app',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
